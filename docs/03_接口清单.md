# XTools OPEN API 接口清单（71 个）

麦可旺志 Microwants · xtools 优化项目 · 2026-09-16（依据 2026-09-01 版文档）

来源：《XToolsCRM OPEN API》apizza 导出（OPEN API_20260901.html）。统一入口 `POST https://crm.xtcrm.com/open/index.xt`，机器可读版本见 `xtools/catalog.json`。序号为新版文档中的顺序（2026-05-22 版为 68 个，序号从“联系人修改接口”起顺延 1 位）。

“首批”列为 ✓ 的模块（客户与联系人、订单与发货、产品与库存、财务）属于首批接入范围；“基础”模块为各模块共用。“版本”列标出本版新增或有变更的接口。


## 基础（6 个，首批：共用）

| 序号 | 接口 | cmd | dt | act | 类型 | 版本 | 要点 |
|---|---|---|---|---|---|---|---|
| 1 | 登录接口 | user.login | — | — | 登录 |  | 换取 sid；10 秒限频 |
| 2 | 用户读取接口 | api.output | user | — | 读取 |  | lastid / id |
| 34 | 附件读取接口 | api.downfile | customer | — | 附件 |  | url 约 2 小时有效 |
| 60 | 字段信息（数据字典）读取接口 | api.fieldinfo | customer | — | 字段信息 |  | field 指定字段 |
| 61 | 字段信息（字段名称）读取接口 | api.fieldinfo | customer | dbcn | 字段信息 |  | act=dbcn |
| 62 | 字段信息（用户）读取接口 | api.fieldinfo | customer | pr2nm | 字段信息 |  | act=pr2nm；part/name 可选 |

## 客户与联系人（14 个，首批：✓）

| 序号 | 接口 | cmd | dt | act | 类型 | 版本 | 要点 |
|---|---|---|---|---|---|---|---|
| 3 | 客户读取接口 | api.output | customer | — | 读取 |  | 过滤 lastid/id/sn/name/lasttime/life/type/cu_status/employees；extend=1 含扩展字段与联系人 |
| 4 | 客户自定义字段数据读取接口 | api.output | customerext | — | 读取 |  | ext_* 自定义字段 |
| 5 | 客户转移接口 | api.chgown | customer | — | 转移 |  | owner 为姓名或 part；id 与 sn 二选一 |
| 6 | 企业客户写入接口 | api.input | customer | — | 写入 |  | 含 contact 联系人数组；extend=1 可写扩展字段 |
| 7 | 企业客户修改接口 | api.update | customer | — | 修改 |  | 只改客户表；id 或 sn 定位 |
| 8 | 企业客户自定义字段数据写入接口 | api.input | customerext | — | 写入 |  | data 含 sn 或 id 与 ext_itemN |
| 9 | 联系人修改接口 | api.update | contact | — | 修改 | 新增 20260901 | 新增：data.id 必填，不能传 cu_sn；示例 extend=1；可改用户画像 ext_selN / ext_itemN |
| 10 | 个人客户写入接口 | api.input | cuview | — | 写入 |  | 个人客户 |
| 22 | 行动记录写入接口 | api.input | action | — | 写入 |  | cale 1 日程;2 待办;3 记录;4 *待办；op_id/prj_id 二选一 |
| 23 | 行动记录读取接口 | api.output | action | — | 读取 |  |  |
| 32 | 销售机会读取接口 | api.output | opport | — | 读取 |  | extend=1 含自定义字段 |
| 65 | 获客线索读取接口 | api.output | jk_collect | — | 读取 |  |  |
| 66 | 获客线索写入接口 | api.input | jk_collect | — | 写入 |  | 返回 id |
| 69 | 下游询价记录写入接口 | api.input | ask_price_note | — | 写入 |  | 含 ask_price_note_item |

## 订单与发货（20 个，首批：✓）

| 序号 | 接口 | cmd | dt | act | 类型 | 版本 | 要点 |
|---|---|---|---|---|---|---|---|
| 11 | 订单写入接口 | api.input | contract | — | 写入 |  | cu_sn 定位客户；goods.prod 为产品编码；extend=1 可用品名/型号/规格/批次匹配 |
| 12 | 订单读取接口 | api.output | contract | — | 读取 |  | 过滤 No./confirm/status/st_send/date 区间/payment；extend=1 |
| 13 | 订单修改接口 | api.update | contract | — | 修改 |  | goods 全量替换；cu_sn 不可改 |
| 14 | 订单分批发货（不计算库存的商品）接口 | api.cmdact | contract | sendGoods_no_stock | 动作 |  | act=sendGoods_no_stock |
| 15 | 订单自定义明细读取接口 | api.cmdact | contract | getOtherItems | 动作 |  | act=getOtherItems |
| 20 | 报价单写入接口 | api.input | price | — | 写入 |  | pricedetail 明细 |
| 21 | 报价单修改接口 | api.update | price | — | 修改 |  | pricedetail 全量替换（无返回示例） |
| 40 | 入库单写入接口 | api.input | libin | — | 写入 |  | 返回 id |
| 41 | 入库单完成入库接口 | api.cmdact | libin | libinok | 动作 |  | act=libinok |
| 42 | 出库单读取接口 | api.output | libout | — | 读取 |  | 过滤 id/lastid/lasttime/mes_work_order_id |
| 43 | 出库单写入接口 | api.input | libout | — | 写入 |  | 返回 id |
| 44 | 出库单完成出库接口 | api.cmdact | libout | liboutok | 动作 |  | act=liboutok |
| 45 | 发货单读取接口 | api.output | sendgoods | — | 读取 |  | memo 可做唯一性校验 |
| 54 | 收发货通知单读取接口 | api.output | sr_notice | — | 读取 |  | 需外部库存；过滤 type/status/lasttime |
| 55 | 收发货通知单状态变更接口 | api.cmdact | sr_notice | chgst | 动作 |  | 需外部库存；act=chgst，只能改为 2/5/6/7 |
| 56 | 收发货通知单执行接口 | api.cmdact | sr_notice | exc | 动作 |  | 需外部库存；act=exc；no_more=1 控制数量 |
| 58 | 合同读取接口 | api.output | contract0 | — | 读取 |  |  |
| 59 | 合同写入接口 | api.input | contract0 | — | 写入 |  |  |
| 68 | 发货单修改接口 | api.update | sendgoods | — | 修改 |  | extend=1；id 或 sn 定位；deli_note |
| 71 | 订单退货单读取接口 | api.output | libreturn | — | 读取 | 新增 20260901 | 新增，只读：主单 + rtnitem；st_libin 入库状态、st_hk 退款状态 |

## 产品与库存（9 个，首批：✓）

| 序号 | 接口 | cmd | dt | act | 类型 | 版本 | 要点 |
|---|---|---|---|---|---|---|---|
| 46 | 产品表读取接口 | api.output | product | — | 读取 |  | 过滤 id/lastid/lasttime/sn/mflag；部分枚举返回展示文本 |
| 47 | 产品写入接口 | api.input | product | — | 写入 |  | 返回 id；ptype 受外部库存开关约束 |
| 48 | 产品修改接口 | api.update | product | — | 修改 |  | id 定位 |
| 49 | 产品客制别名读取接口 | api.output | prod_alias | — | 读取 |  | 过滤 cu_id/pid |
| 50 | 产品客制别名写入接口 | api.input | prod_alias | — | 写入 |  | name 与 sn 至少一项 |
| 51 | 产品价格策略变更接口 | api.cmdact | product | chg_str_ps | 动作 |  | act=chg_str_ps |
| 52 | 产品分类写入接口 | api.input | csstree | — | 写入 |  | 返回 id |
| 53 | 产品分类读取接口 | api.output | csstree | — | 读取 |  |  |
| 57 | 外部库存变更接口 | api.cmdact | libn | erp_libn | 动作 |  | 需外部库存；act=erp_libn |

## 财务（15 个，首批：✓）

| 序号 | 接口 | cmd | dt | act | 类型 | 版本 | 要点 |
|---|---|---|---|---|---|---|---|
| 16 | 采购单写入接口 | api.input | purchase | — | 写入 |  | extend=1；puritem 明细 |
| 17 | 采购单读取接口 | api.output | purchase | — | 读取 |  | 含 puritem |
| 18 | 付款计划读取接口 | api.output | pay_plan | — | 读取 |  |  |
| 19 | 付款计划写入接口 | api.input | pay_plan | — | 写入 |  | cu_sn 为供应商编号 |
| 26 | 计划回款读取接口 | api.output | gathering | — | 读取 |  | memo 可做唯一性校验 |
| 27 | 计划回款修改接口 | api.update | gathering | — | 修改 |  | cu_sn 不可改 |
| 28 | 计划回款写入接口 | api.input | gathering | — | 写入 |  |  |
| 29 | 回款记录写入接口 | api.input | gathering_note | — | 写入 |  | date/money/type 必填；返回 id |
| 30 | 回款记录读取接口 | api.output | gathering_note | — | 读取 |  | memo 可做唯一性校验 |
| 31 | 开票记录写入接口 | api.input | bill | — | 写入 |  | 返回 id |
| 35 | 批单报销修改接口 | api.update | cost | — | 修改 |  | aprv_status=4/5 终结审批 |
| 36 | 批单报销读取接口 | api.output | cost | — | 读取 |  | 过滤 lasttime/baox_st1/aprv_status |
| 37 | 费用明细读取接口 | api.output | costdetail | — | 读取 |  |  |
| 67 | 开票申请的开票动作接口 | api.cmdact | bill_apply | kaipiao | 动作 |  | act=kaipiao |
| 70 | 采购退货单读取接口 | api.output | purreturn | — | 读取 | 新增 20260901 | 新增，只读：主单 + purrtnitem；status 0 待处理;2 执行中;3 结束;4 终止 |

## 售后与其他（7 个，首批：—）

| 序号 | 接口 | cmd | dt | act | 类型 | 版本 | 要点 |
|---|---|---|---|---|---|---|---|
| 24 | 维修工单写入接口 | api.input | repairinfo | — | 写入 |  | 含 repairinfo 副表 |
| 25 | 维修工单读取接口 | api.output | repairinfo | — | 读取 | 变更 20260901 | 本版补充返回字段：主单 + repairinfo + goods（配件及服务明细） |
| 33 | 流程读取接口 | api.output | process_exec | — | 读取 |  | db_name + db_id |
| 38 | 出差读取接口 | api.output | b_trip | — | 读取 |  |  |
| 39 | 项目读取接口 | api.output | project | — | 读取 |  |  |
| 63 | 领料单预生成接口 | api.cmdact | mes_process | preview | 动作 |  | act=preview |
| 64 | 生产工艺工序 | api.output | mes_flow | — | 读取 |  |  |

## 统计

| 类型 | 数量 |
|---|---|
| 读取 | 27 |
| 写入 | 20 |
| 动作 | 10 |
| 修改 | 8 |
| 字段信息 | 3 |
| 登录 | 1 |
| 转移 | 1 |
| 附件 | 1 |

说明：附录“环境变量”“状态码”在导出文件中仍为空（侧栏有“状态码”条目但无正文）；正文出现的错误码为 20101 / 20102（session 失效）与 30103（登录限频）。
